import 'package:e_commerce_project/data/data_source/static/static.dart';import 'package:flutter/material.dart';import 'package:get/get.dart';import 'package:e_commerce_project/core/constant/app_routes_name.dart';abstract class OnBoardingController extends GetxController {  next();  onPageChanged(int index);}class OnBoardingControllerImp extends OnBoardingController {  late PageController pageController;  @override  next() {    currentPage++;    if (currentPage > onBoardingList.length-1)    {Get.offAllNamed(AppRouteName.sign_in);    }    pageController.animateToPage(currentPage,        duration: Duration(milliseconds: 900), curve: Curves.easeInOutExpo);    // if (currentPage >=0 && currentPage <= onBoardingList.length)    // {    // }    // TODO: implement next    // throw UnimplementedErrorNext();  }  int currentPage = 0;  @override  onPageChanged(int index) {    // TODO: implement onPageChanged    currentPage = index;    update();    // throw UnimplementedError();  }  @override  void onInit() {    pageController = PageController();    super.onInit();  }}class UnimplementedErrorNext {}